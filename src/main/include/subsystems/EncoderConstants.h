#pragma once

// ==========================================================================

class EncoderConstants {
public:
	static const double COUNTS_PER_TURN;
	static const double QUARTER_TURN;
	static const double HALF_TURN;
	static const double THREEQUARTER_TURN;
	static const double FULL_TURN;
};

// ==========================================================================
